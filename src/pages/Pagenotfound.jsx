//Page when wrong url is typed in continuation to home Url

import { ErrorPage } from "../components/ErrorPage"
export const Pagenotfound = () => {
    return (
        <section>
            <ErrorPage />
        </section>
    )
}
